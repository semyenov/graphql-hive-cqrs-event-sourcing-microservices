/**
 * Federation examples and demonstrations
 * 
 * These are example files - run them directly:
 * - bun run src/examples/yoga-federation-server.ts
 * - bun run src/examples/run-federation-example.ts
 */

// Re-export for convenience only
export * from './federation-example'
export { createYogaServer } from './yoga-federation-server'